export enum TicketProviderStatus {
  Active = 'active',
  Inactive = 'inactive',
}

export enum TicketProviderUserIdentifier {
  Email = 'email',
  PhoneNumber = 'phoneNumber',
}

export enum TicketProviderSecurityLevel {
  Level1 = 1,
  Level2 = 2,
}
